> **AIエージェントへの指示:**
> このタスクリストを実行する際は、必ず以下のドキュメントと最新のソースコードを参照してください。
> - 仕様書: `stamp_creation_architecture.md`
>
> また、タスクはチェックボックスの**1項目（サブタスク）**ずつ実行してください。
> 私が次のサブタスクの実行を指示するまで、複数のタスクを同時に進めないでください。

# フェーズ3: 追加機能と永続化対応 ToDo

## タスク3.1: 設定機能の実装
- [ ] アプリケーション内に設定モーダルまたは設定ページを追加する。
- [ ] 設定項目として「テキスト画像の高さ（px）」を追加し、数値を入力できるようにする。
- [ ] 設定値が変更されたら、`localStorage`に保存し、アプリケーション起動時に読み込むようにする。
- [ ] `createTextImage`関数が、この設定値を参照して画像を生成するようにする。

## タスク3.2: デバッグ用情報の表示機能
- [ ] `Sticker`オブジェクトの型定義に、生成時情報を保存するプロパティ（例: `generationPayload: { prompt: string, images: string[] }`）を追加する。
- [ ] 画像生成時に使用したプロンプトと入力画像（キャラクター画像とテキスト画像のBase64データ）を、このプロパティに保存する。
- [ ] `StickerCard.tsx`コンポーネントに、デバッグ用アイコンボタンを追加する。
- [ ] ボタンをクリックするとモーダルが開き、保存されたプロンプトテキストと入力画像2枚を表示する。