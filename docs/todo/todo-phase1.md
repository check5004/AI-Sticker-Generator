> **AIエージェントへの指示:**
> このタスクリストを実行する際は、必ず以下のドキュメントと最新のソースコードを参照してください。
> - 仕様書: `stamp_creation_architecture.md`
>
> また、タスクはチェックボックスの**1項目（サブタスク）**ずつ実行してください。
> 私が次のサブタスクの実行を指示するまで、複数のタスクを同時に進めないでください。

# フェーズ1: UIと軽微な機能改善 ToDo

## タスク1.1: スタンプ個数選択の仕様変更
- [x] `StickerCreationTab.tsx`内のスタンプ個数選択UIを、4個から48個まで4個単位で増減できるUIに変更する。
- [x] 選択された個数が状態管理に正しく反映されることを確認する。

## タスク1.2: 生成設定サジェストの拡充とAIによる追加生成機能
### 1. 静的なサジェストの追加
- [x] `StickerCreationTab.tsx`で定義されている「口調・雰囲気」および「文字の装飾」の初期表示Chipリストを拡充する。

### 2. AIによる動的サジェスト生成機能
- [x] 各サジェストChipリストの末尾に「もっと読み込む」ボタンを設置する。
- [x] ボタンクリック時にローディング状態を示し、`geminiService`に新しい関数 `generateSuggestions` を追加する。
- [x] 新サービスで`gemini-2.5-flash-lite`モデルを利用し、既存のサジェストを考慮した上で、新たな提案を複数生成して返すように実装する。
- [x] 返却された新しいサジェストを既存のChipリストに追加して表示する。

## タスク1.3: 自由指示テキストエリアの設置
- [ ] 生成設定パネル（`StickerCreationTab.tsx`）内に、自由記述のデザイン指示用テキストエリアを配置する。
- [ ] このテキストエリアの内容をstateで管理し、テキスト生成および画像生成APIを呼び出す際に、プロンプトの一部として組み込めるようにする。