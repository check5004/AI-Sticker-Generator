> **AIエージェントへの指示:**
> このタスクリストを実行する際は、必ず以下のドキュメントと最新のソースコードを参照してください。
> - 仕様書: `docs/sticker_creation_flow.md`
>
> また、タスクはチェックボックスの**1項目（サブタスク）**ずつ実行してください。
> 私が次のサブタスクの実行を指示するまで、複数のタスクを同時に進めないでください。

# フェーズ5: テキスト画像の正方形化対応 ToDo

## タスク5.1: テキスト画像の正方形化と中央配置
- [x] `services/imageUtils.ts`内の`createTextImage`関数を修正する。具体的には、テキスト幅から計算された`canvasWidth`の値を、そのまま`canvas.height`にも設定する。これにより、Canvas自体を正方形にする。既存の高さ計算ロジック（`Math.max(imageHeight, totalTextHeight + padding * 2)`）は、この新しい正方形化ロジックで置き換える。テキストの描画位置（特にY座標）の計算が、新しい正方形の高さでも引き続き中央になることを確認する。